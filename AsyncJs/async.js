const body = document.querySelector("body");

// alert("THis is a synchronous code");
//web api

setTimeout(() => console.log("async func"), 3000); //asynchronous
body.classList.add("change_green");

const arr = [2, 4, 5];
arr.map((item) => {
  console.log(item);
});
console.log("hi");
const request = new XMLHttpRequest();
rsole.log(err));
